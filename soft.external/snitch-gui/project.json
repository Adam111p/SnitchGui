{
  "name": "snitch-gui",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "snitch-gui/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project snitch-gui --web",
  "targets": {
    "serve": {
      "executor": "@nx/vite:dev-server",
      "options": {
        "proxyConfig": "snitch-gui/proxy.conf.json"
      }
    }
  }
}
