import { createTheme } from '@mui/material/styles';

export const customTheme = createTheme({
  palette: {
    primary: { main: '#28fb94' },
    secondary: { main: '#9c27b0' },
    background: { default: '#f3f4ec' },
  },
});